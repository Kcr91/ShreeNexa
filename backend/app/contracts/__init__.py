"""Cross-process contracts: shared types/tables no single process owns.

Per docs/decisions/0004, this namespace exists only because a feature needs
it -- F0.3 needs a durable, process-independent way to prove liveness.
"""
